
    var inboxEventGosia = this._mysqlEvents.add(
    'gammu.inbox.SenderNumber.value',
    function (oldRow, newRow, event) {
        //if(oldRow == null)
        {
            that._connection.query("INSERT INTO outbox (DestinationNumber, TextDecoded) VALUES ('+48784027936','Dzieki Gosiu');", function (error, results, fields) {
            if (error) {
                winston.log('error', error.stack);
            }
        })
        }
    },
    "+48784027936"
    );